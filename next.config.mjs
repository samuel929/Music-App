/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['i1.sndcdn.com','content-eu-live-global.prod.marketing.bat.net'], // Add the domain you need here
      },
    
};

export default nextConfig;
